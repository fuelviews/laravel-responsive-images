<img
     src="{{ $imgsrc }}"
     srcset="{{ $srcset }}"
     alt="{{ $altText }}"
     class="{{ $class }}"
     width="{{ $imageWidth }}"
     height="{{  $imageHeight }}"
     loading="{{ $loading }}"
>
